# tasks.md — GEAR FIGHT 開発タスクリスト

優先度: 🔴高 / 🟡中 / 🟢低。着手前に `CLAUDE.md`「エンジン構成」を確認し、旧エンジン（ストーリーモード）と新エンジン（`src/combat/`、今後の主軸）のどちらの作業か見極めてから進める。完了したらチェックし、区切りの良いところでコミットを提案する。

## 🔴 新エンジン（`src/combat/`、開発の主軸）

- [ ] **戦闘の手触りの仕上げ**：2026-07-23にヘッドレスブラウザで`★対戦・アーケード（新エンジン）`→`TrainingScene`を2回に分けて詳しく操作して確認。`docs/gearfriends_design.md`の「現状は幾何学プレースホルダ」という記述はやや古く、**実際には`PuppetRig`ベースの実スプライトで既に動いている**（`判定:非表示`という箱表示トグルがあるだけ）。
  - **確認できたこと**：移動・弱攻撃（ヒット時に拳が正しく接触しHP/超必ゲージが反応）・投げ（着弾エフェクトあり）・ギアシフト（G+でGL1→GL2、HUD表示が連動）・**試合開始（`試合開始 ▶`ボタン）で本物のラウンド制**（`ROUND 1`バナー・HP全回復・勝敗ピップ表示）・**波動枠必殺技（スティック中立+必殺ボタン、touch特有のSmash方式入力）が発動**（ただし専用VFXがまだ簡易な箱/円形プレースホルダ）・**超必殺技（スティック↓+必殺ボタン）が発動**し、命中時にキャラがピンク系にフラッシュ・多段ヒットで相手HPを大きく削る演出も確認・**継続攻撃で相手HPを完全に0まで削り切れる**（ラウンド決着条件到達を確認、ただしK.O.バナー自体のスクリーンショットは取得できず）。コンソール/ページエラーは合計100回超の入力を通して0件
  - **小さな不具合を発見**：ダメージ量のポップアップ数字（ヒット時に浮かぶ数値）が、HUD上部の「判定:表示/非表示」トグルラベルの上に重なって表示される（zオーダー/配置の問題）。複数回のヒットで再現するので実際に直してよい程度の確度
  - **依然未確認**：昇竜枠必殺技（→前方+必殺ボタン、2回試したが1回目は方向を上と誤認識・2回目はラウンド開始バナー中に入力してしまい両方ともテスト側のミスで未検証）、ガードの成否（後ろ入力を保持した状態でも相応にダメージを受けた＝しゃがみ低段技等の中段/下段の読み合いで抜けられた可能性があり、実装バグと断定はできない）、CPU以外のAIスタイル（`CombatAI.ts`の`turtle`/`zone`/`trick`/`rush`）の挙動差—今回は1体のCPU（`rush`寄りと思われる「CPU:攻める」表記の相手）としか対戦していない
  - 「何を合格ラインとするか」は依然ユーザー判断が必要。次に触る人は上記未確認項目、特に昇竜技とAIスタイル差を優先して詰めるとよい
- [ ] **`docs/sprite_generation_prompts.md`を新エンジンのポーズ表に合わせて更新**（`docs/gearfriends_design.md`§4手順1）。現行のprompts.mdは旧エンジン（16bitドット絵、ポーズ構成も異なる）向けのまま
- [ ] **ハジメくんからHD 2Dスプライトを1体ずつ本番投入**（`docs/gearfriends_design.md`§4手順2-4）：生成→緑背景切り出し→足元原点配置→`drawFighter`を箱の上にスプライトを重ねる方式に拡張（当たり判定は箱のまま、見た目と分離）。まずハジメ1体を通しで載せて全ポーズ・全技が破綻しないか検証してから横展開。手触りの仕上げが先行タスク

## 🟡 旧エンジン（ストーリーモード、保守・仕上げ）

- [ ] 各キャラ専用AIプロファイルの個別実装。`src/ai/AIController.ts`は`kakashi`/`sonica`の2択のみで、ganrock/aegis/drift/theorion/omeganova/sophislegionは全て`sonica`ロジックを暫定流用中（`src/scenes/BattleScene.ts`の`aiProfile`分岐）。新エンジンの`CombatAI.ts`（`AIStyle`＋キャラ別`AI_PROFILES`）が参考になる
- [ ] 第1〜5章・隠しボス戦の「章開始デモ」会話を執筆（オープニングと最終章は執筆済み）。`DialogueScene`/`postWinDialogue`基盤は完成しているので、`src/data/`に章ごとの`DialogueLine[]`を追加し`ModeSelectScene`から繋ぐだけで拡張可能
- [ ] 隠しボス（ノギ先生/ソフィス・レギオン）戦のストーリー導線（会話・登場演出）。スプライト自体は組み込み済み
- [ ] カカシくんの画風統一：対応方針は決定済み（他キャラと同じ塗り込み画風でプロンプト再調整）、`kakashi_v2.png`の生成待ち。生成後は「新キャラを追加する手順」3〜7に沿って組み込む
- [ ] クイズ・ちしきゲージ・パーツ損傷・ピットイン（パーツ換装／ガレージ自体は実装済み）
- [ ] ノックダウン後の起き上がり無敵が無い（OTGで無防備な拘束が起き得る、無限ではないが要検討）

## 🟢 フェーズ3・4（両エンジン共通の将来構想）

- [ ] 殿堂入り編・ギア検定・環境イベント（フェーズ3）
- [ ] Firebaseオンライン対戦（フェーズ4）。現状Firebase設定はプレースホルダのみで`.env`未設定でも動作する状態を維持

## 参考：ドキュメント
- `docs/仕様書.md` — 教科書対応の学習設計（両エンジン共通の上位仕様）
- `docs/gearfriends_design.md` — 新エンジンのスプライト仕様・キャラの動きの個性づけ・HD 2D生成プロンプト雛形
- `docs/sprite_generation_prompts.md` — 旧エンジン（16bitドット絵）の生成プロンプト集。新エンジン向けの更新は上記タスク参照
- `CLAUDE.md`「エンジン構成」節 — 新旧エンジンの対応表
